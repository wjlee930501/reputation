# 2026-09-07 A01~A17 부분 운영 전환 기록

문서 버전: **1.0** · 기록일: **2026-09-07 (Asia/Seoul)**
런타임 소스: **`eb555180752a66753917f837419498bdcdce5606`**
CI 설정 전용 소스: **`7446762`**
성격: **부분 전환 기록. migration·Worker·FAQ 수리까지 완료했으며 전체 배포 완료 기록이 아니다.**

## 소스·검증·빌드

- PR #82가 런타임 merge commit `eb555180752a66753917f837419498bdcdce5606`으로 병합됐다. 이 소스의 로컬 Backend 전체 결과는 3,084 passed, 0 failed, 0 skipped, 19 warnings, coverage 83.06%, 98.20초이며 실제 PostgreSQL·Redis·native PDF와 populated `0064` → `0069` upgrade를 포함한다.
- PR #82 CI의 9개 check는 성공했지만 Backend에서 TASK22 PostgreSQL 검사가 환경 별칭 때문에 1건 skip됐다. 런타임을 바꾸지 않고 CI의 `TASK22_DATABASE_URL`만 교정한 `7446762`의 [PR #83 CI](https://github.com/wjlee930501/reputation/actions/runs/34117637771)는 9/9 성공했고 Backend 3,084 passed, 0 skipped, 20 warnings, coverage 83.07%, 238.08초였다.
- Cloud Build `0e4abe74-c6b5-453c-aae5-dd9acc67f8bc`는 SUCCESS이며 런타임 merge commit을 OCI revision label로 사용했다.

| 이미지 | digest |
|---|---|
| `reputation` | `sha256:220039f6f19b89bbd1d52cc0da79f65ea3e229d6c4cf34a6512f6e202a0ca18a` |
| `site` | `sha256:6e6e32d462ec2ff33f09d97986611fdb3f912ef16da63df05612a79a7d03f124` |
| `admin` | `sha256:121456629eb88055ade8df9e4b735d4eef6809c3a78aeb8227045fe8fd4224c8` |

## 완료된 운영 변경

| 단계 | 결과 |
|---|---|
| Migration | `reputation-migrate-grl2d` 성공, 운영 DB head `0069_content_first_publication` 확인 |
| Worker | `reputation-worker-00141-wgm`에 Backend digest 배포, 트래픽 100% |
| 공개 기준 고정 | 7개 tenant의 공개 글 115건 exact 기준과 본문 재검수 22건 allowlist 고정. 레거시 기준 허용 ABSENT 92건·PASS 1건은 유료 일괄 재검수에서 제외 |
| FAQ 구두점 수리 | exact 3건 CAS 적용, 즉시 replay 변경 0건. 세 글은 `DEFERRED`이나 ABSENT 레거시이며 22건 유료 재검수 allowlist에는 추가하지 않음 |
| 이미지 read-only dry-run | exact 115건 candidate, current 0, remaining 115 확인. 공급자 호출·인증·교체·CAS writeback 없음 |
| 전환 전 공개 표면 | 구 API 기준 7개 tenant·공개 글 115건 누락 0건과 tenant별 대표 이미지 7건 HTTP 성공 확인 |

5개 상시 서비스의 revision·release·traffic을 다시 읽은 결과 Worker만 `eb55518`로 100% 전환됐다. API·Beat·Site·Admin은 기존 `345a642` revision에서 각각 100% 트래픽을 유지한다. 전환 전 공개 표면 확인은 새 API나 새 Site가 정상이라는 증거가 아니다.

## 외부 호출 승인 차단과 남은 단계

본문 exact 22건의 실제 독립 검수는 Anthropic에 병원·본문·공식 자료 맥락을 보내는 실행이었다. 자동 도구 승인 검토가 이 운영 payload의 외부 전송에 대한 구체적 동의가 없다는 이유로 호출을 거부했고 사용자 승인을 기다리고 있다. 이번 릴리스용 본문 검수 Job의 요청은 공급자에 도달하지 않았다.

이미지 exact 115건의 실제 바이트 인증도 Google에 이미지 바이트와 글 제목(주제)을 보내야 하므로 같은 명시적 실행 승인이 필요하다. read-only dry-run만 수행했고 이번 릴리스용 이미지 검수 Job의 공급자 호출, 인증, content-addressed 불변 복사, 교체, CAS 저장은 실행하지 않았다. 이 범위는 기존 정상 운영 스케줄의 공급자 호출 여부에 대한 주장이 아니다.

이 승인은 실행 에이전트가 운영 맥락을 외부 공급자에 보내기 위한 세션 권한이다. 서비스의 발행 승인이나 제품의 새 HITL 절차가 아니며 정상 생성·검수·복구에 영구적인 사람 승인 단계를 추가하지 않는다.

남은 순서는 다음과 같다.

1. 승인을 받은 뒤 본문 exact 22건 독립 재검수를 종결한다.
2. 이미지 exact 115건을 실제 바이트로 검수하고 불변 복사 또는 안전한 교체, byte-bound 인증과 CAS 저장을 종결한다.
3. 본문·이미지 보완 뒤 공개 API 전환 전에 exact 115 ID의 새 엄격 공개 gate와 인증 hash 기반 이미지 URL 버전을 read-only로 검증한다.
4. RedBeat `2026-09-07.2` 재조정, Beat, production readiness와 7개 queue canary를 실행한다.
5. 그 뒤 API·Site·Admin을 배포하고 실제 revision·공개 표면·로그를 재검증한다.

현재 API·Site·Admin·Beat는 새 이미지로 배포하지 않았고 RedBeat 재조정·readiness·control canary도 실행하지 않았다. 이미지 115건은 새 계약으로 인증되지 않았다. 따라서 이 기록을 전체 배포 완료나 A01~A17 운영 종결로 해석하지 않는다.

상세 exact ID와 실행 JSON·로그는 추적하지 않는 `/private/tmp/reputation-autonomy-release` 아래에 보관했다. 완료된 일회성 preflight·FAQ repair·image plan Cloud Run Job 3개는 증거를 남긴 뒤 삭제했으며 상시 서비스와 기존 영속 Job은 이 정리로 바꾸지 않았다. 이 문서에는 비밀값과 공개 콘텐츠 UUID 목록을 넣지 않았다.
